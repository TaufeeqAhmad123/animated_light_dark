import 'package:flutter/material.dart';

const sunColor = Color(0xFFFABA50);
const moonColor = Color(0xFFFFFFFF);
const dayBGColor = Color(0xFFF8E6C2);
const nightBGColor = Colors.black;
const animationDuration = Duration(milliseconds: 700);
const animationCurve = Curves.fastOutSlowIn;
const double circleRadius = 120;
const double chainInitialHeight = 80;
const double chainThickness = 2;
const double chainHandleRadius = 16;
const chainHandleHoldDuration = Duration(seconds: 0);
const chainHandleReleaseDuration = Duration(seconds: 1);
const chainHandleAnimationCurve = Curves.elasticOut;
